version: 2
jobs:
  build:
    machine: true
    steps:
    - checkout
    - run: echo "hello world"
workflows:
  version: 2
  build_and_test:
    jobs:
    - build
  nightly:
    triggers:
    - schedule:
        cron: '0 * * * *'
        filters:
          branches:
            only:
            - feature/kburns_fix_cypress_cron2
    jobs:
    - build
