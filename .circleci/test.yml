version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2 # the primary container, where your job's commands are run
    steps:
    - checkout # check out the code in the project directory
    - run: echo "Default test" # run the `echo` command
  build2:
    docker:
      - image: circleci/node:4.8.2 # the primary container, where your job's commands are run
    steps:
      - checkout # check out the code in the project directory
      - run: echo "Default test (2)" # run the `echo` command
workflows:
  version: 2
  build-and-test:
    jobs:
      - build:
          context: ANDURIL_POLY_CACHE
  combine:
    triggers:
      - schedule:
          cron: "* * * * *"
          filters:
            branches:
              only:
                - jpr/schedule-with-require
    jobs:
      - build:
          context: ANDURIL_POLY_CACHE
      - build2:
          requires:
            - build
